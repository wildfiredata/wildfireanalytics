# For more information on configuration, see:
#   * Official English Documentation: http://nginx.org/en/docs/
#   * Official Russian Documentation: http://nginx.org/ru/docs/

user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

# Load dynamic modules. See /usr/share/doc/nginx/README.dynamic.
include /usr/share/nginx/modules/*.conf;

events {
    worker_connections 1024;
}

http {
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile            on;
    tcp_nopush          on;
    tcp_nodelay         on;
    keepalive_timeout   65;
    types_hash_max_size 4096;

    include             /etc/nginx/mime.types;
    default_type        application/octet-stream;

    # Load modular configuration files from the /etc/nginx/conf.d directory.
    # See http://nginx.org/en/docs/ngx_core_module.html#include
    # for more information.
    include /etc/nginx/conf.d/*.conf;

    server {
        # 分析网站
        listen       80;
        listen       [::]:80;
        server_name  192.168.3.25;
        root         /usr/share/nginx/html;

        # Load configuration files for the default server block.
        include /etc/nginx/default.d/*.conf;

        error_page 404 /404.html;
        location = /404.html {
        }

        error_page 500 502 503 504 /50x.html;
        location = /50x.html {
        }
    }

    # 如果需要添加HTTPS支持，可以参考以下配置

#     server {
#         # 分析网站
#         listen       443 ssl http2;
#         listen       [::]:443 ssl http2;
#         server_name  192.168.3.25;
#         #root         /usr/share/nginx/html;
#         root         /usr/share/nginx/chacha;


#         #请填写证书文件的相对路径或绝对路径
#         ssl_certificate ./certs/analytics.galaxyok.cn_bundle.crt; 
#         #请填写私钥文件的相对路径或绝对路径
#         ssl_certificate_key ./certs/analytics.galaxyok.cn.key; 
#         ssl_session_timeout 5m;
#         #请按照以下协议配置
#         ssl_protocols TLSv1.2 TLSv1.3; 
#         #请按照以下套件配置，配置加密套件，写法遵循 openssl 标准。
#         ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE; 
#         ssl_prefer_server_ciphers on;
        
# 	# Load configuration files for the default server block.
#         include /etc/nginx/default.d/*.conf;

#         error_page 404 /404.html;
#             location = /40x.html {
#         }

#         error_page 500 502 503 504 /50x.html;
#             location = /50x.html {
#         }
#     }

#     server {
#         # APIGATE HTTP (分析网站服务器API地址)
#         listen       28080 ssl; 
#         server_name  192.168.3.25; 
        
#         #请填写证书文件的相对路径或绝对路径
#         ssl_certificate ./certs/analytics.galaxyok.cn_bundle.crt; 
#         #请填写私钥文件的相对路径或绝对路径
#         ssl_certificate_key ./certs/analytics.galaxyok.cn.key; 
#         ssl_session_timeout 5m;
#         #请按照以下协议配置
#         ssl_protocols TLSv1.2 TLSv1.3; 
#         #请按照以下套件配置，配置加密套件，写法遵循 openssl 标准。
#         ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE; 
#         ssl_prefer_server_ciphers on;

# #        root         /usr/share/nginx/html;
#         location / {  # nginx 转发
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_pass http://0.0.0.0:8388;   # APIGATE 服务的HTTP endpoint
#         }

#         error_page 404 /404.html;
#             location = /40x.html {
#         }

#         error_page 500 502 503 504 /50x.html;
#             location = /50x.html {
#         }
#     }

#     server {
#         # TRACKGATE HTTP (sdk上报数据服务器API地址)
#         listen       25502 ssl; # SDK 上报track数据端口
#         server_name  192.168.3.25; # SDK 上报track数据地址
        
#         #请填写证书文件的相对路径或绝对路径
#         ssl_certificate ./certs/analytics.galaxyok.cn_bundle.crt; 
#         #请填写私钥文件的相对路径或绝对路径
#         ssl_certificate_key ./certs/analytics.galaxyok.cn.key; 
#         ssl_session_timeout 5m;
#         #请按照以下协议配置
#         ssl_protocols TLSv1.2 TLSv1.3; 
#         #请按照以下套件配置，配置加密套件，写法遵循 openssl 标准。
#         ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE; 
#         ssl_prefer_server_ciphers on;

#         location / {  # nginx 转发
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_pass http://0.0.0.0:8390;  # TRACKGATE 服务的HTTP endpoint
#         }

#         # wss 升级协议配置
#         location /wss/trackgate/ {  # nginx 转发
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;   # 升级协议头
#             proxy_set_header Connection upgrade;

#             proxy_pass http://0.0.0.0:8390; # TRACKGATE 服务的HTTP endpoint
#         }

#         error_page 404 /404.html;
#             location = /40x.html {
#         }

#         error_page 500 502 503 504 /50x.html;
#             location = /50x.html {
#         }
#     }
}

